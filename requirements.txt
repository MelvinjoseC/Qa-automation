# ==============================================================================
# Engineering QA Automation & CAD Suite Dependencies
# ==============================================================================

# --- Application 1: ISO Project Folder Auditor (Audit.py) ---
python-docx>=1.1.0,<2.0.0     # Word document register parsing
reportlab>=4.1.0,<5.0.0       # Compliance PDF report building

# --- Application 2: STEP to Geometry BOM App (app.py) ---
# NumPy 2.0 introduces binary/C-API changes that are incompatible with older
# OCP / CadQuery builds. Pin to <2.0.0 to ensure environment stability.
numpy>=1.22.0,<2.0.0          # Vector operations and Principal Component Analysis

# Note on cadquery/OCP installation:
# OCP binary wheels are platform-specific and best installed via conda/mamba:
#   conda install -c cadquery -c conda-forge cadquery
# Alternatively, you can use pip if pre-compiled wheels exist for your environment:
#   pip install cadquery

# --- Development and Quality Assurance ---
pytest>=7.4.0,<9.0.0          # Automated test execution
pytest-cov>=4.1.0,<6.0.0      # Code coverage reporting
ruff>=0.2.0,<1.0.0            # High-performance linter/formatter
